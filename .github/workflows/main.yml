name: Daily Price Check

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:
    inputs:
      password:
        description: 'Admin password for test email (optional)'
        required: false
        default: ''
      skip_scraper:
        description: 'Skip scraping step (set to true for testing detector)'
        required: false
        default: 'false'

permissions:
  contents: write

jobs:
  scrape-and-alert:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests beautifulsoup4 playwright
          playwright install chromium
      - name: Sync subscribers
        env:
          GUMROAD_ACCESS_TOKEN: ${{ secrets.GUMROAD_ACCESS_TOKEN }}
          FORMSPREE_API_KEY: ${{ secrets.FORMSPREE_API_KEY }}
        run: python sync_subscribers.py
      - name: Run scraper
        if: github.event.inputs.skip_scraper != 'true'
        run: python test_engine.py
      - name: Run price detector
        env:
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          ADMIN_PASSWORD_INPUT: ${{ github.event.inputs.password }}
          ADMIN_SECRET: ${{ secrets.ADMIN_TEST_PASSWORD }}
        run: python price_detector.py
        
      - name: Run personalised watchlist checker
        env:
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python watchlist_checker.py
      - name: Commit updated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add books_today.csv paid_subscribers.json free_subscribers.json watchlist.json
          git diff --staged --quiet || git commit -m "Update prices and subscriber lists [skip ci]"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/dealkly/deal-alerts.git
